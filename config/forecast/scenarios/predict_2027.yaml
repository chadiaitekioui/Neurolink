# Inference 2027: LoRA frozen at year_max_2025, context includes partial 2026 (N-1).
# No ground truth for 2027 — predict only, no eval.
db_path: data/neurolink.db
test_years: [2027]
top_k: [10, 50]
models:
  - literature_lora
seed: 42
literature:
  adapter_dir: data/models/literature
  benchmark_lora_year_max: 2025
  max_context_questions: 20
  backend: lora
  use_4bit: true
  braingpt_model: BrainGPT/BrainGPT-7B-v0.2
  training_prompt_k: 1
  generation_mode: batch
  filter_outputs: true
  llm:
    max_new_tokens: 96
    tokens_per_direction: 48
    temperature: 0.0
    top_p: 1.0
    seed: 42
